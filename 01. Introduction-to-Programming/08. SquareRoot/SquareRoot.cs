/*
 * Create a console application that calculates and prints the square root of the number 12345.
 */

using System;

class SquareRoot
{
    static void Main()
    {
        double squareRoot = Math.Sqrt(12345d);
        Console.WriteLine(squareRoot);
    }
}
